# .env.example — 复制为 .env 后填写
# 本项目零依赖，不需要 API key 也能运行核心流程。
# 以下为可选配置（如你的 DSH 环境需要自定义）

# 可选：DeepSeek/LLM API 配置（占位符，仅示例）
DEEPSEEK_API_KEY=your-api-key-here
DEEPSEEK_BASE_URL=https://api.example.com/v1

# 可选：输出目录覆盖
OUTPUT_DIR=./output

# 可选：临时目录
TEMP_DIR=./_deepread_temp